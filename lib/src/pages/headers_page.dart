import 'package:disenios2/src/widgets/headers.dart';
import 'package:flutter/material.dart';
class HeadersPages extends StatelessWidget {
  const HeadersPages({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: HeaderOla()
    );

  }
}